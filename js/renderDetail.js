function setFavicon(letter = 'N') { const canvas = document.createElement('canvas'); canvas.width = 64; canvas.height = 64; const ctx = canvas.getContext('2d'); ctx.fillStyle = '#2563eb'; ctx.font = 'bold 40px Arial, sans-serif'; ctx.textAlign = 'center'; ctx.textBaseline = 'middle'; ctx.fillText(letter, 32, 34); const icon = document.getElementById('favicon'); if (icon) icon.href = canvas.toDataURL('image/png'); }
function esc(value) { return String(value ?? '').replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])); }
function tags(tags = []) { return tags.map(tag => `<span class="tag">${esc(tag)}</span>`).join(''); }
function relatedCards(items, currentId) { return items.filter(n => n.id !== currentId).slice(0,3).map(n => `<a class="related-card" href="detail.html?id=${encodeURIComponent(n.id)}"><div class="related-rank">${esc(n.rank)}</div><div class="related-title">${esc(n.title)}</div></a>`).join(''); }
async function renderDetail() { setFavicon('N'); const main = document.getElementById('main-content'); const crumb = document.getElementById('breadcrumb-name'); try { const id = new URLSearchParams(location.search).get('id'); const response = await fetch('../data/news.json', { cache: 'no-cache' }); if (!response.ok) throw new Error(`HTTP ${response.status}`); const data = await response.json(); const item = (data.items || []).find(n => n.id === id) || (data.items || [])[0]; if (!item) throw new Error('empty data'); document.title = `${item.title} | 今日新闻详情`; if (crumb) crumb.textContent = item.rank; main.innerHTML = `<header class="detail-header"><div class="header-row"><span class="rank">${esc(item.rank)}</span><span class="source">${esc(item.source)}</span>${item.badge ? `<span class="badge">${esc(item.badge)}</span>` : ''}</div><h1 class="detail-title">${esc(item.title)}</h1><p class="lead">${esc(item.summary)}</p><div class="detail-meta"><div class="meta-item"><span class="meta-label">来源</span><span class="meta-value">${esc(item.source)}</span></div><div class="meta-item"><span class="meta-label">日期</span><span class="meta-value">${esc(item.date)}</span></div><div class="meta-item"><span class="meta-label">时间</span><span class="meta-value">${esc(item.time)}</span></div><div class="meta-item"><span class="meta-label">编号</span><span class="meta-value">${esc(item.rank)}</span></div></div></header><section class="section"><h2 class="section-title">摘要</h2><div class="section-content"><p>${esc(item.summary)}</p><div class="tag-row">${tags(item.tags)}</div><div class="action-row"><a class="btn" href="${esc(item.originalUrl)}" target="_blank" rel="noopener noreferrer">打开原始报道 →</a><a class="btn" href="../index.html#news-list">返回新闻目录 →</a></div></div></section><section class="section"><h2 class="section-title">关键看点</h2><div class="section-content"><ul class="point-list">${(item.points || []).map(point => `<li>${esc(point)}</li>`).join('')}</ul></div></section><section class="section"><h2 class="section-title">背景</h2><div class="section-content"><p>${esc(item.background)}</p></div></section><section class="section"><h2 class="section-title">可能影响</h2><div class="section-content"><p>${esc(item.impact)}</p></div></section><section class="section"><h2 class="section-title">相关推荐</h2><div class="section-content"><div class="related">${relatedCards(data.items || [], item.id)}</div></div></section>`; } catch (error) { console.error(error); main.innerHTML = '<div class="error">详情数据加载失败，请返回目录重试。</div>'; } }
document.addEventListener('DOMContentLoaded', renderDetail);
